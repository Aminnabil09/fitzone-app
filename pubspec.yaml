name: fitzone_app
description: FitZone - Your Ultimate Sports Store
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  shared_preferences: ^2.2.2
  carousel_slider: ^5.0.0
  shimmer: ^3.0.0
  google_fonts: ^8.0.2
  google_generative_ai: ^0.4.7
  image_picker: ^1.2.1
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.9
  firebase_auth: ^5.5.2
  firebase_storage: ^12.4.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

